# Copy to .env and fill values or set them in your runner/secrets/variables
DISCORD_WEBHOOK=your_discord_webhook_url_here
TARGET_URL=https://ugcleaks.short-term.workers.dev/leaks
CHECK_INTERVAL_SECONDS=60
DATA_FILE=./seen.json
HEADLESS=true
# Optional: set RUN_ONCE=true for one-time runs (useful in CI/GitHub Actions)
RUN_ONCE=false
